---
import Base from '../../layouts/Base.astro';
import { getRelativeLocaleUrl } from 'astro:i18n';
const installUrl = getRelativeLocaleUrl('zh', '/install/');
const pluginsUrl = getRelativeLocaleUrl('zh', '/plugins/');
const tutorialUrl = getRelativeLocaleUrl('zh', '/tutorial/');
---
<Base
  title="DeepSeek Harness 排错指南 — 常见 dsh 报错修复 | dshbase 中文"
  description="修复 DeepSeek Harness（dsh）常见报错：ERR_PNPM_FETCH_404、ERR_REQUIRE_ESM、插件不激活、MISSING_CREDENTIAL、pnpm 缺失、allowBuilds 等——全部来自真实实测的已验证修复。"
  currentPath="/troubleshooting/"
>
  <section class="hero wrap">
    <p class="eyebrow">排错</p>
    <h1>修复常见 <span class="accent">dsh</span> 报错</h1>
    <p class="lede">下面的每个修复都来自真实运行 dsh 0.1.0-rc.6——不是照抄文档。每条都给出精确报错、原因和一行修复命令。</p>
  </section>

  <section class="info wrap">
    <h2>安装与环境</h2>

    <h3><code>ERR_PNPM_FETCH_404</code> — 包找不到</h3>
    <p>你的 npm 镜像落后于官方，刚发布的包返回 404。</p>
    <pre class="code-block"><code>dsh plugin add &lt;包名&gt; --registry=https://registry.npmjs.org</code></pre>

    <h3><code>'pnpm' 不是内部或外部命令</code></h3>
    <p><code>dsh plugin</code> 会调用 pnpm，但本机没装。</p>
    <pre class="code-block"><code>npm install -g pnpm</code></pre>

    <h3><code>node: command not found</code></h3>
    <p><code>dsh</code> 启动器需要 Node.js 在 PATH 里。把 Node 目录（和 npm 全局 bin）加进 PATH，用 <code>npm prefix -g</code> 查路径。</p>

    <h3>3080 端口被占用</h3>
    <p>旧 dsh 进程占着端口——页面甚至能正常加载，但连的是旧实例，配置改动永远不生效。</p>
    <pre class="code-block"><code>dsh web --port 8080</code></pre>
    <p>先杀掉旧进程，或用新端口。</p>

    <h2 style="margin-top:28px">凭证与模型</h2>

    <h3><code>MISSING_CREDENTIAL</code></h3>
    <p>没存 API key。在 <strong>设置 → 模型</strong> 填，或写进 <code>~/.dsh/.credentials.yaml</code>：</p>
    <pre class="code-block"><code>DEEPSEEK_API_KEY: sk-你的key</code></pre>

    <h3><code>UNKNOWN_MODEL</code></h3>
    <p>选了个没配置的模型。给提供方添加该模型，或选已配置的模型。</p>

    <h3>「获取可用模型」返回 401</h3>
    <p>key 不对——模型发现会调用 OpenAI 兼容的 <code>GET /models</code> 端点。基础 URL、key、模型名三者必须匹配。</p>

    <h2 style="margin-top:28px">插件（实测验证）</h2>

    <h3>插件装了但不激活</h3>
    <p>包没有 <code>dsh.bundle</code> 清单。没有它，<code>dsh plugin add</code> 只当普通依赖装——永远不加载。插件必须带 <code>cordis.patch.yml</code> 并在 <code>package.json</code> 声明 <code>dsh.bundle.patch</code>。</p>

    <h3>插件加载时 <code>ERR_REQUIRE_ESM</code></h3>
    <p>插件被编译成 CommonJS，但依赖了 ESM-only 的包（如 <code>@deepseek-ai/dsh-tools</code>）。插件必须构建成 ESM（<code>"type": "module"</code>）。</p>

    <h3><code>cannot get property "systemPrompt" without inject</code></h3>
    <p>插件代码调了 <code>ctx.systemPrompt</code> 却没声明 <code>inject: ["systemPrompt"]</code>。这是插件 bug——把这条报错原样发给作者。</p>

    <h3>git 插件提示 <code>allowBuilds</code></h3>
    <p>从 GitHub 装的插件带 <code>prepare</code> 构建脚本，pnpm 默认阻止。把 pnpm 提示的 key 加进 <code>pnpm-workspace.yaml</code> 的 <code>allowBuilds</code>，再重跑。</p>

    <h3>依赖「<code>@deepseek-ai/dsh-type-meta</code>」找不到</h3>
    <p>插件依赖了还没发到 npm 的包。客户端无解——作者得先发布。</p>

    <h2 style="margin-top:28px">运行时</h2>

    <h3>Web UI 发送按钮一直灰</h3>
    <p>web profile 要先选工作区才能发送。点<strong>选择工作区</strong>并选中目录。</p>

    <h3>技能在 CLI 能用、Web UI 不能用</h3>
    <p>web profile 默认<strong>禁用</strong>了 <code>tool-skill</code> 和 <code>skill-filesystem</code>；headless/CLI profile 是开的。同一个技能一个能用、另一个静默无效。</p>

    <p style="margin-top:28px">还没解决？看<a href={installUrl}>安装指南</a>、<a href={pluginsUrl}>插件指南</a>或<a href={tutorialUrl}>教程</a>。</p>
  </section>
</Base>

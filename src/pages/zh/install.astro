---
import Base from '../../layouts/Base.astro';
import { getRelativeLocaleUrl } from 'astro:i18n';
const tutorialUrl = getRelativeLocaleUrl('zh', '/tutorial/');
const pluginsUrl = getRelativeLocaleUrl('zh', '/plugins/');
---
<Base
  title="DeepSeek Harness 安装与下载教程 — 官方获取渠道 + 一键安装（Windows / macOS / Linux / WSL）| dshbase 中文"
  description="DeepSeek Harness 安装与下载完整指南：官方发行渠道（npm 包 @deepseek-ai/dsh / GitHub 源码）、版本与 SHA 校验方法，Windows、macOS、Linux、WSL 一键安装。只需 Node.js，然后 npx @deepseek-ai/dsh web。"
  currentPath="/install/"
>
  <script type="application/ld+json" set:html={JSON.stringify({
    "@context": "https://schema.org",
    "@type": "FAQPage",
    mainEntity: [
      {
        "@type": "Question",
        name: "DeepSeek Harness 怎么下载？有官方安装包吗？",
        acceptedAnswer: { "@type": "Answer", text: "DeepSeek Harness（DSH）没有独立的 Windows/macOS 安装包（.exe/.dmg）。它是 Node.js 包，官方下载渠道只有两个：npm（@deepseek-ai/dsh，用 npm install -g @deepseek-ai/dsh 获取）和 GitHub 源码仓库（deepseek-ai/deepseek-harness，git clone 后用 pnpm 构建）。" }
      },
      {
        "@type": "Question",
        name: "「deepseek harness安装」和「deepseek harness下载」有什么区别？",
        acceptedAnswer: { "@type": "Answer", text: "下载指获取程序本体（npm 包或源码），安装指把 Node.js 运行环境和 dsh 命令配置到位。DSH 的官方流程里两步合一：npx @deepseek-ai/dsh web 会自动下载并运行最新版，无需单独下载安装包。" }
      },
      {
        "@type": "Question",
        name: "如何校验下载的 DSH 包完整性？",
        acceptedAnswer: { "@type": "Answer", text: "运行 npm view @deepseek-ai/dsh dist.integrity 查看当前版本的 SHA-512 完整性值；通过 npm/npx 安装的包由 npm 客户端自动校验。手动下载的 .tgz 可用 npm pkg 或 sha512sum 比对。" }
      },
      {
        "@type": "Question",
        name: "deepseekharness 连写或 deepseek harness安装（无空格）搜到的是一回事吗？",
        acceptedAnswer: { "@type": "Answer", text: "是。deepseekharness、deepseek_harness、DSH 都是同一个项目 DeepSeek Harness 的常见写法，本页覆盖其下载与安装全流程。" }
      }
    ]
  })} />
  <section class="hero wrap">
    <p class="eyebrow">安装 · 下载</p>
    <h1>DeepSeek Harness 安装与下载 <span class="accent">（DSH 安装 · DSH 下载）</span></h1>
    <p class="lede">DeepSeek Harness（DSH）是一个 Node.js 包——唯一的要求就是 Node.js。先看清官方发行渠道与校验方式（下载），再选你的系统一键安装，或从源码构建。装好后见 <a href="/zh/blog/dsh-rc8/">rc.8 枢纽</a>。</p>
  </section>

  <section class="info wrap">
    <h2>DeepSeek Harness 下载：官方发行渠道</h2>
    <p>DSH <strong>不提供独立的 .exe / .dmg 安装包</strong>——任何第三方"DSH 安装包下载"站都不是官方渠道。官方获取方式只有两条：</p>
    <ul>
      <li><strong>npm 发行包</strong>：包名 <code>@deepseek-ai/dsh</code>（当前 <strong>0.1.1-rc.2</strong>，开发者预览迭代中）。安装命令见下文；只想拿压缩包的话，直接下载 registry 的 tarball：<a href="https://registry.npmjs.org/@deepseek-ai/dsh/-/dsh-0.1.1-rc.2.tgz" target="_blank" rel="noopener">registry.npmjs.org/@deepseek-ai/dsh/-/dsh-0.1.1-rc.2.tgz</a>（换版本号即可取对应版本）。</li>
      <li><strong>GitHub 源码</strong>：<a href="https://github.com/deepseek-ai/deepseek-harness" target="_blank" rel="noopener">github.com/deepseek-ai/deepseek-harness</a>（官方仓库，Releases 页提供版本归档）。</li>
    </ul>
    <p style="margin-top:10px"><strong>版本与 SHA 校验</strong>——下载前后核对包完整性：</p>
    <pre class="code-block"><code>npm view @deepseek-ai/dsh dist.integrity   # 输出 sha512-… 完整性值
npm view @deepseek-ai/dsh version          # 当前最新版号</code></pre>
    <p>经 <code>npm install</code> / <code>npx</code> 安装的包由 npm 客户端自动做完整性校验（对照 registry 的 SHA-512），无需手动核对；只有从非 npm 渠道拿到的 <code>.tgz</code> 才需要自己比对 <code>sha512sum</code>。</p>

    <h2 style="margin-top:28px">开始之前</h2>
    <ul>
      <li>需要 <strong>Node.js LTS（18+）</strong>——下面每个脚本都会先检查它。</li>
      <li>DeepSeek Harness 处于<strong>开发者预览</strong>阶段——迭代很快，<strong>未来会有破坏性变更</strong>。</li>
      <li>想要离线版？<a href="/deepseek-harness-guide.pdf">下载 DeepSeek Harness 指南 PDF</a>（安装 · 插件 · 排错 · vs Claude Code）。</li>
    </ul>

    <h2 style="margin-top:28px">Windows — 一键（PowerShell）</h2>
    <p>打开 <strong>PowerShell</strong>（右键开始 → 终端）粘贴：</p>
    <pre class="code-block"><code>if (-not (Get-Command node -ErrorAction SilentlyContinue)) &#123; winget install OpenJS.NodeJS.LTS &#125;
npx @deepseek-ai/dsh web</code></pre>
    <p>如果没有 <code>winget</code>，从 <a href="https://nodejs.org" target="_blank" rel="noopener">nodejs.org</a> 下载 LTS 安装包手动装 Node.js，再运行 <code>npx @deepseek-ai/dsh web</code>。</p>

    <h2 style="margin-top:28px">macOS — 一键（终端）</h2>
    <pre class="code-block"><code>command -v node &gt;/dev/null 2&gt;&amp;1 || brew install node
npx @deepseek-ai/dsh web</code></pre>
    <p>没有 <a href="https://brew.sh" target="_blank" rel="noopener">Homebrew</a> 的话先装它，或从 nodejs.org 装 Node.js。</p>

    <h2 style="margin-top:28px">Linux — 一键（bash）</h2>
    <p>Debian / Ubuntu（用 NodeSource）：</p>
    <pre class="code-block"><code>command -v node &gt;/dev/null 2&gt;&amp;1 || (curl -fsSL https://deb.nodesource.com/setup_lts.x | sudo -E bash - &amp;&amp; sudo apt-get install -y nodejs)
npx @deepseek-ai/dsh web</code></pre>
    <p>其他发行版、或想避免 <code>sudo</code>，用 <a href="https://github.com/nvm-sh/nvm" target="_blank" rel="noopener">nvm</a>，然后 <code>npx @deepseek-ai/dsh web</code>。</p>

    <h2 style="margin-top:28px">WSL（Ubuntu on WSL）— 一键</h2>
    <p>在 WSL 的 Ubuntu 终端里：</p>
    <pre class="code-block"><code>command -v node &gt;/dev/null 2&gt;&amp;1 || (curl -fsSL https://deb.nodesource.com/setup_lts.x | sudo -E bash - &amp;&amp; sudo apt-get install -y nodejs)
npx @deepseek-ai/dsh web</code></pre>

    <h2 style="margin-top:28px">从源码构建（所有平台通用）</h2>
    <pre class="code-block"><code>git clone https://github.com/deepseek-ai/deepseek-harness.git
cd deepseek-harness
pnpm install
pnpm run build
pnpm dsh web</code></pre>
    <p>使用 <code>pnpm</code>——没有的话先 <code>npm install -g pnpm</code>。</p>

    <h2 style="margin-top:28px">验证运行</h2>
    <p>打开 <strong><code>http://127.0.0.1:3080</code></strong>，填入 DeepSeek API Key。要换端口，传给应用即可：</p>
    <pre class="code-block"><code>npx @deepseek-ai/dsh web --port 8080</code></pre>

    <h2 style="margin-top:28px">安装为全局命令</h2>
    <p><code>npx</code> 每次启动都会重新解析包。要长期使用——尤其是 CLI / 无头模式——建议一次性安装 <code>dsh</code> 命令：</p>
    <pre class="code-block"><code>npm install -g @deepseek-ai/dsh
dsh --version   # 例如 0.1.0-rc.6
dsh web</code></pre>
    <p>确保 npm 全局 bin 目录在你的 <code>PATH</code> 里（用 <code>npm prefix -g</code> 查看）。如果用的是便携版 Node（无全局安装），把 npm 的 <code>prefix</code> 指向你控制的目录（<code>npm config set prefix D:\dsh\npm-global</code>），并把 Node 目录和全局 <code>bin</code> 目录都加进用户 PATH。</p>

    <h2 style="margin-top:28px">配置 API key</h2>
    <p>Web UI 在 <strong>设置 &rarr; 模型</strong> 里存 key。CLI / 无头模式则写在 <code>$DSH_HOME/.credentials.yaml</code>（默认 <code>~/.dsh/.credentials.yaml</code>）：</p>
    <pre class="code-block"><code>DEEPSEEK_API_KEY: sk-your-key-here</code></pre>
    <p>macOS / Linux 上要把文件设为 <code>chmod 600</code>——DSH 会拒绝其他用户可读的凭证文件。<code>DEEPSEEK_API_KEY</code> 环境变量同样生效且优先级更高。</p>

    <h2 style="margin-top:28px">安装排错</h2>
    <ul>
      <li><strong><code>ERR_PNPM_FETCH_404</code> / 包找不到</strong>——默认 npm 镜像可能落后于官方。加 <code>--registry=https://registry.npmjs.org</code> 重试。</li>
      <li><strong><code>'pnpm' 不是内部或外部命令</code></strong>——插件管理会调用 pnpm；先 <code>npm install -g pnpm</code>。</li>
      <li><strong><code>node: command not found</code></strong>——Node.js 不在 PATH 里。把 Node 目录（和 npm 全局 bin）加进 PATH。</li>
      <li><strong>3080 端口被占用</strong>——换端口启动：<code>dsh web --port 8080</code>。</li>
    </ul>

    <h2 style="margin-top:24px">下一步</h2>
    <ol class="steps">
      <li>按<a href={tutorialUrl}>教程</a>配置模型并运行第一个任务。</li>
      <li>在 <strong>设置 → 模型</strong> 填入 DeepSeek API Key。</li>
      <li>用<a href={pluginsUrl}>插件指南</a>开发自己的插件，或浏览<a href="/zh/plugins/directory/">全部实测插件目录</a>。</li>
    </ol>
  </section>
</Base>

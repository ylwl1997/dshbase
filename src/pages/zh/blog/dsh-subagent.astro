---
import BlogPost from '../../../layouts/BlogPost.astro';
import { getRelativeLocaleUrl } from 'astro:i18n';
const directoryUrl = getRelativeLocaleUrl('zh', '/plugins/directory/');
const sidechainUrl = getRelativeLocaleUrl('zh', '/plugins/dsh-sidechain/');
const crosstalkUrl = getRelativeLocaleUrl('zh', '/plugins/dsh-crosstalk/');
const sgmeUrl = getRelativeLocaleUrl('zh', '/plugins/dsh-sgme/');
const dataAgentUrl = getRelativeLocaleUrl('zh', '/plugins/dsh-data-agent/');
const interconnectUrl = getRelativeLocaleUrl('zh', '/plugins/dsh-interconnect/');
const productSubagentsUrl = getRelativeLocaleUrl('zh', '/plugins/dsh-plugin-product-subagents/');
const crewUrl = getRelativeLocaleUrl('zh', '/plugins/dsh-crew/');
const claudeBridgeUrl = getRelativeLocaleUrl('zh', '/plugins/dsh-plugin-claude-bridge/');
const codexBridgeUrl = getRelativeLocaleUrl('zh', '/plugins/dsh-codex-bridge/');
const pluginCcUrl = getRelativeLocaleUrl('zh', '/plugins/dsh-plugin-cc/');
const harnessSkillUrl = getRelativeLocaleUrl('zh', '/plugins/DeepSeek-Harness-Skill/');
const delegateUrl = getRelativeLocaleUrl('zh', '/plugins/dsh-delegate/');
---
<BlogPost title="DeepSeek Harness Subagent 教程（rc.8）— Claude Code / Codex 子代理 | dshbase 中文" description="DeepSeek Harness Subagent / DSH 子代理教程（rc.8）：Claude Code / Codex 作 worker、预设、fork、消息通道，以及已验证桥与角色预设（product-subagents、dsh-crew 等）。" currentPath="/blog/dsh-subagent/" category="指南">
  <h1>DeepSeek Harness <span class="accent">子代理</span>（rc.8）：不离开 harness 的多 Agent</h1>
  <p class="muted small" style="margin-bottom:20px">2026 年 8 月 20 日 · dshbase · 对齐 0.1.0-rc.8</p>

  <p>DeepSeek Harness 内核上仍是单 Agent 循环——但 <strong>0.1.0-rc.8</strong> 把多 Agent 故事讲响了：社区科普里 Claude Code / Codex 常被写成可当子代理 / worker，插件目录里也已经有对应的已验证桥与角色预设。成形的不是一条神圣的 <code>spawn_subagent()</code> API，而是可组合的<strong>预设、fork、消息通道与外部 Agent 桥</strong>。下面是零件怎么拼，并标明安装边界。</p>

  <h2>为什么还是没有单一的 spawn 命令</h2>
  <p>如果你来自自带 spawn API 的框架，dsh 会显得不一样——而且是故意的。内核是插件系统，不是编排运行时，多 Agent 靠原语拼出来。驱动一次性侧聊的 fork，也支撑消息交接；定义 Data Agent 的预设，也可以定义 Claude Code 形的 worker。没有预装全家桶——要自己组装。</p>

  <h2>Claude Code / Codex 作子代理（rc.8 口径）</h2>
  <p>rc.8 报道常说可以把 <strong>Claude Code</strong>、<strong>Codex</strong> 当子代理。实操上通常是三种形态——请选清楚，别混营销标签：</p>
  <ul>
    <li><strong>在 dsh 内的角色预设</strong> — <a href={productSubagentsUrl}>dsh-plugin-product-subagents</a> 提供基于角色的 Codex / Claude Code / ACP 子代理预设（已验证）。你仍在 harness 里；「Claude/Codex」更多是人设与工具策略，不一定再起第二个二进制。</li>
    <li><strong>从 Claude Code / Codex 派活进 dsh</strong> — <a href={crewUrl}>dsh-crew</a> 让 Claude Code / Codex 把工作派给 DSH Agent（进度、机内 worker、分档预设）。反向桥 <a href={pluginCcUrl}>dsh-plugin-cc</a> 把 DSH 嵌进 Claude Code 做评审与委派。<a href={harnessSkillUrl}>DeepSeek-Harness-Skill</a> 是 Codex/Claude 侧「把这任务交给 DSH」的 skill 入口。</li>
    <li><strong>配置 / CLI 桥</strong> — <a href={claudeBridgeUrl}>dsh-plugin-claude-bridge</a> 把 Claude Code 的记忆、技能与配置拉进 DSH；<a href={codexBridgeUrl}>dsh-codex-bridge</a> 暴露 Codex CLI 宿主工具与 Web 会话页。这是迁移 / 互操作缝，不是魔法共享内存。</li>
  </ul>
  <p><strong>安装与边界：</strong>每座桥都要自备凭据（Anthropic / OpenAI / ChatGPT OAuth——以该插件文档为准）。当外部 Agent 能写文件时，工作区权限档位要诚实（只读 vs 完全访问）。安装命令以目录里已验证页为准；高 star 的 pending 不等于干净 profile 实测通过。若要在 dsh 内做按模型派生子调用，见 <a href={delegateUrl}>dsh-delegate</a>。</p>

  <h2>Agent 预设：按角色拆子代理</h2>
  <p>harness 内最干净的入口仍是<strong>预设</strong>。<strong><a href={dataAgentUrl}>dsh-data-agent</a></strong> 定义专用 Data Agent，负责查、改、分析数据。不是把所有技能塞进一个编程人设——任务偏数据时切过去。product-subagents 把同一思路延伸到 Claude/Codex 形角色。</p>

  <h2>基于 fork 的侧会话</h2>
  <p>轻量并行用 <strong><a href={sidechainUrl}>dsh-sidechain</a></strong>：<code>/side</code>（持久）与 <code>/btw</code>（一次性）在临时 fork 里跑，「不写入主会话历史」。主会话只追加日志仍可审计；fork 继承父上下文。目录里已验证（常见是 <code>git-build</code> 而非 npm）。</p>

  <h2>跨会话与跨实例消息</h2>
  <ul>
    <li><strong><a href={crosstalkUrl}>dsh-crosstalk</a></strong> — 本机任意会话可互列、互发，Claude Code 风格。</li>
    <li><strong><a href={interconnectUrl}>dsh-interconnect</a></strong> — 跨实例消息 / 事件交接，靠 interconnect 服务 + 工具。</li>
  </ul>
  <p>两者加起来，一群 dsh 实例就能派活、回传，不必在标签页之间复制粘贴。</p>

  <h2>跨 Agent 共享记忆</h2>
  <p><strong><a href={sgmeUrl}>dsh-sgme</a></strong> 是「通过 HTTP 做多 agent 共享长期记忆」的桥。消息搬任务；共享记忆让交接不丢上下文。仍早期（星数低）但已验证——目录里仍是最明确对准多 agent 的记忆槽。</p>

  <h2>模型切换：另一个「多」的杠杆</h2>
  <p>「子代理」也可以指同一个 harness、换模型：路由用便宜/快，重活用更强，按会话或任务切。再叠预设（以及真正需要第二运行时时的 Claude/Codex 桥），就是实用分工，而不必假装存在一条万能 spawn。</p>

  <h2>在 rc.8 上拼起来</h2>
  <ol>
    <li>要在 dsh <em>内</em> 要 Claude/Codex 形 worker → <strong>dsh-plugin-product-subagents</strong>（可选 <strong>dsh-delegate</strong>）。</li>
    <li>要 Claude Code / Codex <em>派活进</em> dsh → <strong>dsh-crew</strong> / <strong>DeepSeek-Harness-Skill</strong> / <strong>dsh-plugin-cc</strong>。</li>
    <li>要一次性并行 → <strong>dsh-sidechain</strong> fork。</li>
    <li>要 Agent 互聊 → 本机 <strong>dsh-crosstalk</strong> / 跨实例 <strong>dsh-interconnect</strong>。</li>
    <li>要共享记忆 → <strong>dsh-sgme</strong>。</li>
  </ol>
  <p>以上都是 Cordis 内核上的插件（或 skill）——所以从早期 rc.6 文到 rc.8，多 Agent 故事推进得这么快。安装命令与当前验证状态，从<a href={directoryUrl}>插件目录</a>开始。</p>
</BlogPost>

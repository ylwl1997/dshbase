---
import BlogPost from '../../../layouts/BlogPost.astro';
import { getRelativeLocaleUrl } from 'astro:i18n';
const directoryUrl = getRelativeLocaleUrl('zh', '/plugins/directory/');
const troubleshootingUrl = getRelativeLocaleUrl('zh', '/troubleshooting/');
const cacheUrl = getRelativeLocaleUrl('zh', '/blog/pi-deepseek-cache/');
const sessionLogUrl = getRelativeLocaleUrl('zh', '/blog/deepseek-harness-session-log/');
---
<BlogPost title="DeepSeek Harness 四种模式 — 标准 / PTC / 极简 / 创造（完整指南）| dshbase 中文" description="DeepSeek Harness 四种预设详解：标准（功能全开编码 Agent）、PTC（程序化工具调用省 token）、极简（bash+编辑器做基准测试）、创造（自进化）。外加哪种模式缓存最优，以及创造模式里要注意的 dsh.bundle 和 allowBuilds 两个坑。" currentPath="/blog/deepseek-harness-modes/" category="指南">
  <h1>DeepSeek Harness 四种模式：<span class="accent">完整指南</span></h1>
  <p class="muted small" style="margin-bottom:20px">2026 年 8 月 14 日 · dshbase</p>

  <p>DeepSeek Harness 内置四种预设。别想复杂了——它们就是<strong>模板预设</strong>，打包了一组插件方便你开箱即用。切换模式改变的是<strong>Agent 手上有什么工具、按什么规则跑</strong>，不是模型能力。下面是每一种，加上我们在 dsh 0.1.0-rc.6 上跑出来的实战细节。</p>

  <h2>1. 标准模式</h2>
  <p>如果你第一次使用、或对底层机制不了解，<strong>无脑选标准模式</strong>。它是功能全开的编码 Agent，插件都预设好了：</p>
  <ul>
    <li>文件读取与编辑</li>
    <li>Shell</li>
    <li>文件搜索与网页搜索</li>
    <li>Skills、计划、目标</li>
    <li>后台任务、子 Agent、工作流</li>
  </ul>
  <p>代价是：这是一个<strong>很大的工具面</strong>，对能力是好事，但对 DeepSeek 的前缀缓存是坏事。每个工具都是每个请求最前面的一块内容，大的工具面更难保持稳定。如果你对成本敏感，这点很重要——下面极简模式会细说。</p>

  <h2>2. PTC 模式（程序化工具调用）</h2>
  <p>PTC 拥有标准模式全部能力——区别在<strong>工具的呈现方式</strong>。标准模式里模型通常发起一次工具调用、拿到结果再决定下一步；PTC 模式会给模型一套 <strong>Code Mode SDK</strong>，让模型写一段 TypeScript 程序，在一次 <code>run_code</code> 里组合多个工具操作。</p>
  <p>原来可能需要五次模型往返的读取、搜索、筛选、并行调用和结果整理，有机会被<strong>压进一次程序执行</strong>。这会减少模型与工具之间来回对话的次数，更适合结构化、多步骤、可并行的操作，同时<strong>更省 token</strong>——一方面往返少了、重发的上下文就少；另一方面中间数据不再回到上下文。</p>
  <p>代价是：需要模型具备足够稳定的代码规划能力，调试难度也高。<strong>小白先别碰，用标准模式就行。</strong>等你遇到大量重复工具往返，或想测试 DeepSeek 模型的程序化工具调用能力，再开 PTC。</p>

  <h2>3. 极简模式</h2>
  <p>极简模式只给模型<strong>两个核心工具</strong>：一个持久 Bash、一个文件编辑器。它还把系统提示词固定成一句非常简单的「你是一个有帮助的软件工程助手」，去掉上下文压缩和大量额外能力。</p>
  <p>这个模式主要用于<strong>最小环境下的模型基准测试</strong>——比较两个模型的裸 Agent 能力，不受 harness 干扰。没事别日常用，你会发现根本没法用。但它同时也是缓存最优的配置：两个工具 + 冻结的提示词，是可能的最小稳定前缀，这正是<a href={cacheUrl}>99.93% 缓存命中率</a>打法背后的配方。想测一个模型的裸成本下限，极简模式就是那个地方。</p>

  <h2>4. 创造模式 ——「现场造扳手」模式</h2>
  <p>创造模式是 DeepSeek Harness 最特别的地方。它拥有标准模式完整能力，还能<strong>检查自己正在运行的 Cordis 环境，在内存中试验插件</strong>——让 Agent 能<strong>直接改造自己</strong>。</p>
  <p>例子：</p>
  <blockquote class="code-block" style="font-style:normal">「帮我做一个只允许读代码、不允许改文件，专门负责安全审计的模式。」</blockquote>
  <blockquote class="code-block" style="font-style:normal">「帮我做一个接入公司内部搜索、固定使用某个模型、拥有三种专属 Skills 的研究 Agent。」</blockquote>
  <p>你甚至可以先让 Agent 检查自己已有的插件，发现缺某个插件的话，<strong>现场创造一个插件，然后直接挂到正在运行的流程中</strong>。比喻：Agent 发现自己没有扳手，于是现场造了一把扳手，插到自己手上，接着用这把扳手继续干活。</p>
  <p>让 Agent 帮你造插件时，有两个我们实测踩过的坑要留意：</p>
  <ul>
    <li><strong>缺 <code>dsh.bundle</code> 清单</strong>——自造插件没有清单会静默地装成普通依赖、永远不激活。信任结果前先核对清单。</li>
    <li><strong><code>allowBuilds</code> 确认</strong>——从 GitHub 装的插件带 <code>prepare</code> 构建脚本时，pnpm 会拦住它等你确认。那个提示是权限边界，不是 bug。</li>
  </ul>
  <p>完整修复见<a href={troubleshootingUrl}>排错指南</a>。</p>

  <h2>怎么理解模式</h2>
  <p>预设就是一个 Agent 所运行的插件集合——工具、提示词、能力。复制一份改改，或用创造模式让 Agent 帮你建。不同预设暴露不同的斜杠命令（极简只有目标命令，标准/代码/创造包含压缩、目标、计划等）。</p>
  <p>预设也是 DSH 对插件疲劳的答案。我们实测 <strong>101 个社区插件</strong>，只有 11 个从 npm 干净装上——其余是 GitHub 源码或需要额外运行时。预设的存在就是为了让你不从那条长尾起步；你从一个打包好的、测过的集合起步，需要时才伸手去<a href={directoryUrl}>插件目录</a>里挑。</p>
  <p>相关：DeepSeek Harness 的会话是<strong>只追加的事件日志</strong>——见我们的<a href={sessionLogUrl}>会话可观测性</a>文章。</p>

  <h2>FAQ</h2>
  <p><strong>新手该选哪个模式？</strong>标准模式。它是功能全开的完整 Agent。PTC 和创造模式加了能力、也加了失败模式；极简模式是给基准测试用的，不是日常干活。</p>
  <p><strong>哪个模式跑起来最便宜？</strong>极简模式，这是设计使然——两个工具 + 冻结提示词是最小的稳定前缀，所以缓存命中最大化。PTC 第二，因为它合并工具往返、让中间数据不进上下文。</p>
  <p><strong>能在会话中途切换模式吗？</strong>模式是改变插件集合的预设，所以切换意味着换一个活动面，而不是原地微调。复制一份最接近的预设再改。</p>
</BlogPost>

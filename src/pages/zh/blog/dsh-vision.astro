---
import BlogPost from '../../../layouts/BlogPost.astro';
import { getRelativeLocaleUrl } from 'astro:i18n';
const directoryUrl = getRelativeLocaleUrl('zh', '/plugins/directory/');
const packsUrl = getRelativeLocaleUrl('zh', '/packs/');
const auditUrl = getRelativeLocaleUrl('zh', '/audit/');
const modelSetupUrl = getRelativeLocaleUrl('zh', '/blog/dsh-model-setup/');
const modlensUrl = getRelativeLocaleUrl('zh', '/plugins/modlens/');
const visionToolkitUrl = getRelativeLocaleUrl('zh', '/plugins/dsh-vision-toolkit/');
const visionRouterUrl = getRelativeLocaleUrl('zh', '/plugins/dsh-vision-router/');
const visionPrimitivesUrl = getRelativeLocaleUrl('zh', '/plugins/dsh-vision-primitives/');
const dropToPathUrl = getRelativeLocaleUrl('zh', '/plugins/dsh-drop-to-path/');
---
<BlogPost title="DeepSeek Harness Vision 教程（rc.8）— Catalog 缺口 vs 已验证场景包 | dshbase 中文" description="DeepSeek Harness Vision / DSH 视觉指南（rc.8）：多模态叙事 vs deepseek-v4-flash-vision-exp 未进 catalog。今天装已验证 vision pack（modlens / dsh-vision-toolkit），不是 Hub 数量。" currentPath="/blog/dsh-vision/" category="指南">
  <h1>DeepSeek Harness <span class="accent">视觉</span>（rc.8）：三种「能看图」不是一回事</h1>
  <p class="muted small" style="margin-bottom:20px">2026 年 8 月 20 日 · dshbase · 按 0.1.0-rc.8 更新</p>

  <p><strong>dsh 0.1.0-rc.8</strong> 之后，不少科普文写成「DeepSeek Harness 已经能看图了」。这句话把三件不同的事揉成了一句：产品叙事、官方模型 catalog、以及真正通过安装实测的插件。这篇是对照文——营销暗示什么、仓库仍扣着什么、你今天该装什么。</p>

  <h2>1. rc.8 的多模态叙事</h2>
  <p>作为 <em>harness</em> 叙事，rc.8 说的是真的：<code>/goal</code> / <code>/plan</code> 一类流程里可以带图、插件发现更像 Agent Store、纯文本 Agent 仍可「借眼睛」。大众文常把它压成「原生视觉已可用」——这就是坑。Harness 能路由像素，不等于 DeepSeek 官方视觉 API 已对日常调用开放。</p>

  <h2>2. Catalog 缺口：<code>deepseek-v4-flash-vision-exp</code></h2>
  <p>官方树里已经能看到实验性视觉接线——例如 <code>deepseek-v4-flash-vision-exp</code>、<code>input: [text, image]</code>——以及说明：<strong>端点就绪前不进正式 catalog</strong>。也就是：脚手架可能在，公开、可点选、可指望的官方视觉 API 还不在。今天不要去配一个「官方 DeepSeek 视觉模型」并指望识图开箱即用。短版现状也写在<a href={modelSetupUrl}>模型接入</a>页。</p>
  <p class="muted small">信息差说清楚：我们引用的是公开 release / agents note 里「endpoint ready 前不进 catalog」的表述，不猜发版日期。等 catalog 点亮，这篇的 CTA 会跟着改——在此之前，插件仍是主路径。</p>

  <h2>3. 已验证路径：vision 场景包</h2>
  <p>在干净 profile 上真正能跑通的，是我们已经做完 L1–L4 的插件路径。请从场景包入手，而不是从 star 数入手：</p>
  <ul>
    <li><strong><a href={`${packsUrl}#vision`}>纯文本模型视觉外挂</a></strong> — <a href={modlensUrl}>modlens</a>（视觉桥 / OCR+布局 JSON）、<a href={visionToolkitUrl}>dsh-vision-toolkit</a>（图片问答、长截图 OCR）、<a href={visionRouterUrl}>dsh-vision-router</a>（路由 / 免费视觉链）。</li>
    <li><strong><a href={`${packsUrl}#artist-creative`}>艺术家 / 创意视觉包</a></strong> — 同一套桥 + toolkit，再加设计 / GenUI 槽位（<code>dsh-openpencil</code>、<code>dsh-genui</code>），适合要创作面而不只 OCR 的人。</li>
  </ul>
  <p>dshbase 上每个 pack 槽位都指向 <code>plugins.json</code> 里 <code>test=verified</code> 的条目。这和 Hub 徽章、「700+ dsh-plugin」商店数量不是同一把尺子。验证怎么定义：见<a href={auditUrl}>审计方法</a>。</p>

  <h2>委托 vs 原生覆盖层（仍然好用的分法）</h2>
  <p>委托型插件把图交给视觉端点（或免费链），再把文字/JSON 交回纯文本模型——<strong>modlens</strong>、<strong>dsh-vision-toolkit</strong>、<strong>dsh-vision-router</strong>。原生覆盖层把定位留在 dsh 内——<strong><a href={visionPrimitivesUrl}>dsh-vision-primitives</a></strong>（Set-of-Mark 网格）。UX 粘合：<strong><a href={dropToPathUrl}>dsh-drop-to-path</a></strong> 把拖拽变成路径，方便文本模型转交视觉工具。默认装 pack；只有要精确「指哪打哪」时再加 primitives。</p>

  <h2>上传被拒仍是另一类问题</h2>
  <p>第三方视觉模型（gpt 系、kimi、o 系列、grok 等）即便本身多模态，经网关上传仍可能失败。那是上游往返问题，不能当成「DeepSeek 官方已经会看图」的反证。走已验证视觉插件，依旧是最稳的绕法。</p>

  <h2>今天怎么选</h2>
  <ul>
    <li><strong>只要「读这张图」</strong> → 装<a href={`${packsUrl}#vision`}>vision 包</a>（先从 <strong>modlens</strong> 起）。</li>
    <li><strong>长截图 / UI diff / Artifacts</strong> → 保留包内的 <strong>dsh-vision-toolkit</strong>。</li>
    <li><strong>路由 / 免费链</strong> → <strong>dsh-vision-router</strong>。</li>
    <li><strong>视觉之上还要设计 / 图表</strong> → <a href={`${packsUrl}#artist-creative`}>艺术家包</a>。</li>
    <li><strong>在等官方 API 视觉</strong> → 盯 catalog；别把 rc.8 营销当成「端点已 live」。</li>
  </ul>

  <h2>结语</h2>
  <p>rc.8 把多模态<em>故事</em>讲响了；正式 vision catalog 仍被闸住。我们实测能交付的是 packs → modlens / dsh-vision-toolkit / dsh-vision-router（及伙伴），干净 profile 上测过——不是 Agent Store 头数。从 <a href={packsUrl}>/packs/</a> 开始；读<a href={auditUrl}>我们怎么审计</a>；安装命令在<a href={directoryUrl}>插件目录</a>。</p>
</BlogPost>

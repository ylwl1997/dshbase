

########## 表54@8733 lines 8728-8812 ##########
信号、FIFOx_RD 信号、FIFOx_RDATA 信号中的 x 为 1 或 2，SmartNC1 和
SmartNC2 使用 FIFO1，SmartNC3 和 SmartNC4 使用 FIFO2，根据当前使用哪一
路 SmartNC，设置 FIFO_WR_SEL 及 FIFO_RD_SEL 对 FIFO 进行复用选择。
      FIFOx_WDATA 数据格式为命令+NC 可能发送的数据，具体格式如下表所
示。
                       表 54 FIFOx_WDATA 数据格式
 数据      数据含义                                  补充说明
 编号
  1      对于短报文:                                固定存在
         子地址，对于控制流NT，使用bit[4:0]；或者短报文模
         式SmartNT访问，使用bit[15:0]；
  2      对于短报文:                                固定存在
         bit[15]: 1,NT发送指令；0，NT接收指令；
                              第 162 页
    Bit[14] :1,状态帧抑制；0，NT发送状态帧；(长报文不
    适用)
    Bit[13:12] :在NC->NT传输类型下，访问网络终端NT
    的数量；01,对1路NT发起访问；10，对2路NT发起访
    问；11，对3路NT发起访问；00，对4路NT发起访问；
    Bit[11] :交换传输优先级设置；
             '0'，低优先级；
             '1'，高优先级；
    Bit[10] :重传使能；
             '0'，重传禁止；
             '1'，重传使能；
    Bit[9] :0，重传1次，1，重传2次；
    bit[8:0]: 当子地址为0时，对应模式码，使用bit[4:0]；
    当子地址非0时对应数据长度信息，范围是1～512字
    节，其中，bit[8:0]值为0，对应512字节，对于其它值，
    数据长度等于原值，不需要转化；
3   Bit[11:0]: 第1路目标NT的ID；                固定存在
    Bit[14:12] : 第1路目的NT类型；
               “000”：SmartNT1；
               “001”：SmartNT2；
               “010”：SmartNT3；
               “011”：SmartNT4；
               “100”：控制流NT；
    bit[15]: 保留；
4   Bit[11:0]: 第2路目标NT的ID；                不固定存在；
    Bit[14:12] : 第2路目的NT类型；               仅适用于NC->NT传输
               “000”：SmartNT1；            类型、且访问网络终端
               “001”：SmartNT2；            NT的数量>=2时。
               “010”：SmartNT3；
               “011”：SmartNT4；
               “100”：控制流NT；
    bit[15]: 保留；
5   Bit[11:0]: 第3路目标NT的ID；                不固定存在；
    Bit[14:12] : 第3路目的NT类型；               仅适用于NC->NT传输
               “000”：SmartNT1；            类型、且访问网络终端
               “001”：SmartNT2；            NT的数量>=3时。
               “010”：SmartNT3；
               “011”：SmartNT4；
               “100”：控制流NT；
    bit[15]: 保留；
6   Bit[11:0]: 第4路目标NT的ID；                不固定存在；
    Bit[14:12] : 第4路目的NT类型；               仅适用于NC->NT传输
               “000”：SmartNT1；            类型、且访问网络终端
               “001”：SmartNT2；            NT的数量=4时。

                            第 163 页
                      “010”：SmartNT3；
                      “011”：SmartNT4；
                      “100”：控制流NT；
            bit[15]: 保留；
 7          bit[15:0]:SmartNC发送的第1～2字节数据;                   不固定存在；
                                                            适用于NC->NT指令；
 8          bit[15:0]:SmartNC发送的第3～4字节数据;                   不固定存在；
                                                            适用于NC->NT指令；
 9          bit[15:0]:SmartNC发送的第5～6字节数据;                   不固定存在；
                                                            适用于NC->NT指令；
     ...    ...                                             不固定存在；
                                                            适用于NC->NT指令；
      注：传输字节长度不包含编号为 1、2 和 3 的数据。

      FIFOx_RDATA 数据存储格式为状态+NC 可能接收的数据，具体格式如下表
所示。
                    表 55 FIFOx_RDATA 数据格式（1 对 1 模式）
           数据编号    数据含义
             1     状态字
                   Bit[15]: 固定为'1'；
                   Bit[14]: 固定为'0'；
                   Bit[13]: 固定为'1'；
                   Bit[12]: 传输异常；
                   Bit[11]: 固定为'0'；
                   Bit[10]: 固定为'0'；


########## 5.2.3@3362 lines 3362-3441 ##########
5.2.3 工作模式配置寄存器
      偏移地址                         说明
       0x02   工作模式配置寄存器
              Bit[0]: 第 1 路控制流 NC 模式使能；
              Bit[1]: 控制流 NT 模式使能；
              Bit[3:2]: 保留；
              Bit[4]: 第 1 路 SmartNC 使能；
              Bit[5]: 第 2 路 SmartNC 使能；
              Bit[6]: 第 3 路 SmartNC 使能；
              Bit[7]: 第 4 路 SmartNC 使能；
              Bit[8]: 第 1 路 SmartNT 使能；
              Bit[9]: 第 2 路 SmartNT 使能；
              Bit[10]: 第 3 路 SmartNT 使能；

                          第 36 页
                 Bit[11]: 第 4 路 SmartNT 使能；
                 Bit[12]:第 2 路控制流 NC 模式使能；
                 Bit[15:13]: 保留。
  Bit[0]: 第 1 路控制流 NC 功能使能,'1'有效;
  Bit[1]: 控制流 NT 功能使能,'1'有效。
  Bit[4]: 第 1 路 SmartNC 功能使能；
  Bit[5]: 第 2 路 SmartNC 功能使能；
  Bit[6]: 第 3 路 SmartNC 功能使能；
  Bit[7]: 第 4 路 SmartNC 功能使能；
  Bit[8]: 第 1 路 SmartNT 功能使能；
  Bit[9]: 第 2 路 SmartNT 功能使能；
  Bit[10]: 第 3 路 SmartNT 功能使能；
  Bit[11]: 第 4 路 SmartNT 功能使能；
  Bit[12]:第 2 路控制流 NC 功能使能。

5.2.4 功能配置寄存器
       偏移地址                              说明
         0x03    功能配置寄存器
                 Bit[0]: A 通道使能配置
                 Bit[1]: B 通道使能配置
                 Bit[2]: 环功能使能
                 Bit[3]: 带宽统计使能
                 Bit[5:4]: 环节点带宽分配设置
                           "00"，本地占用发送带宽 6.25%；
                           "01"，本地占用发送带宽 12.5%；
                           "10"，本地占用发送带宽 25%；
                           "11"，本地占用发送带宽 50%；
                 Bit[7:6]:帧传输延迟超时设置；
                          "00"，16us；
                          "01"，32us；
                          "10"，64us；
                          "11"，128us。
                 Bit[8]:环网络无效帧主动清除功能使能；
                 Bit[9]:环网络故障状态检测使能；
                 Bit[10]:IDLE 原语检测超时清字同步状态功能禁止，对外保
                 留；
                 Bit[11]:链路建立原语检测超时清字同步状态功能禁止，对外
                 保留；
                 Bit[13:12]:端口处于非活动态超时时间设置，
                          "00"，256ms；
                                第 37 页
                        "01"，512ms；
                        "10"，1024ms；
                        "11"，2048ms。
               Bit[14]: 端口处于非活动态超时事件复位 serders 功能禁止，
               对外保留；
               Bit[15]: serders 原始接收数据记录功能使能；

  Bit[0]: A 通道使能配置，‘1’有效，‘0’，禁止；用于使能或关闭通信链路；
  Bit[1]: B 通道使能配置，‘1’有效，‘0’，禁止；用于使能或关闭通信链路；
  Bit[2]: 环功能使能，当网络终端接入网网络时，使能环功能，否则，禁止环功能；
  Bit[3]: 带宽统计使能，配置为‘1’，对两路光纤通道进行独立的接收带宽统计，配
置为‘0’时，禁止接收带宽统计功能；
  Bit[5:4]: 环节点带宽分配设置
       "00"，本地占用发送带宽 6.25%；
       "01"，本地占用发送带宽 12.5%；
       "10"，本地占用发送带宽 25%；
       "11"，本地占用发送带宽 50%；
  Bit[7:6]: 帧传输延迟超时设置；
       "00"，16us；
       "01"，32us；
       "10"，64us；
       "11"，128us。
  Bit[8]:环网络无效帧主动清除功能使能，仅用于不适用桥接器的单环网络，且该单
环网络仅使能一个接点用于清除无效帧；


########## fmt@3865 lines 3835-3924 ##########
                        0，不使能；

                               第 47 页
                          1，使能；
                 Bit[7]:存储器管理增强模式配置；
                          0，普通模式；
                          1，增强模式；
                 Bit[8]:模式命令增强模式配置；
                          0，普通模式；
                          1，增强模式；
                 Bit[9]:保留；
                 Bit[10]: 保留；
                 Bit[11]:非法命令数据接收禁止配置；当命令非法时，是否禁止数据接
                 收写入到 RAM 区；
                          0，不禁止；
                          1，禁止；
                 Bit[12]:忙状态时交换数据接收禁止配置；当命令处于忙位时，是否
                 禁止数据接收写入到 RAM 区；
                          0，不禁止；
                          1，禁止；
                 Bit[13]:保留；
                 Bit[14]:双缓冲使能配置；
                 Bit[15]:服务请求位自动清除配置。
   Bit[2]:忙位设置是否对模式命令有效配置；若本位配置为‘0’、busy#(控制流 NT 配
置寄存器 2 中 Bit[2]配置为 0）或者命令忙位查找表使能且 busy 配置表中相应位配置为
‘1’时，对于接收的模式命令，NT 返回 BUSY 位置 1、且不带数据负荷的状态帧；若本
位配置为‘1’、busy#(控制流 NT 配置寄存器 2 中 Bit[2]配置为 0）或者命令忙位查找表使
能且 busy 配置表中相应位配置为‘1’时，对于接收的模式命令，NT 返回 BUSY 位置 1、
且带相应数据负荷的状态帧；
   Bit[3]: 在单缓冲/双缓冲模式下，地址遇到 512Byte 边界时是否禁止配置；若本位
配置为‘0’，在子地址配置为单缓冲/双缓冲模式下，GLink 分配给子地址数据存储容量
为固定值(512 字节)；在 GLink 进行与一次交换相对应的数据存储器访问、且存储器当
前访问地址为 18'hx_xx_ff 时，GLink 进行下一个访问的地址跳至 18'hx_xx_00；若本位
配置为‘1’，GLink 当前访问数据存储器的地址为 18'hx_xx_ff 时，GLink 进行下一个访
问的地址将直接递增 1，即{(10'hx_xx+1),8'h00}；
   Bit[4]: 保留；
   Bit[5]: 命令非法查找表功能是否禁止配置；GLinkNT 允许用户对于每个子地址进
行合法数据访问长度范围进行配置，长度范围为[1，512]字节；若接收到交换的数据字
节数不在用户配置的范围内，NT 判当前命令非法；
   Bit[6]: 命令忙位查找表是否使能配置；若本位配置为‘0’,控制流 NT 返回状态帧中
                            第 48 页
busy 位仅由 busy#(控制流 NT 配置寄存器 2 中 Bit[2]）决定;若本位配置为‘1’, 控制流
NT 返回状态帧中 busy 位置为 1 的条件包括 busy#(控制流 NT 配置寄存器 2 中 Bit[2]）
置 0,或者 busy 配置表中相应位配置为‘1’；
   Bit[7]: 存储器管理增强模式配置；若存储器管理增强模式配置为‘0’，控制流 NT
各子地址存储模式配置为单交换数据缓存模式或双交换数据缓存模式；对于发送子地址
数据，存储类型为单交换数据缓存模式；对于接收地址数据，当双缓冲使能位(控制流
NT 配置寄存器 1 的 bit[14])配置为 0 时，存储类型为单交换数据缓存模式，否则为双交
换数据缓存模式；
   当存储器管理增强模式配置为‘1’时，控制流 NT 子地址数据存储可配置为单交换数
据缓存模式、双交换数据缓存模式以及环形数据存储模式（支持存储深度为 2048 字节
至 131072 字节）；其中，双交换数据缓存模式仅适用于接收子地址，不适用于发送子
地址；
   Bit[8]: 模式命令增强模式配置；若模式命令增强模式配置为‘0’，用户可通过使能
控制流 NT 模式命令中断使能（中断屏蔽寄存器的 Bit[9]）来使能针对全部模式命令的
接收中断；发送模式命令和接收模式命令使用公共的数据存储地址区域；
   若模式命令增强模式配置为‘1’，可以通过配置模式命令中断选择表来产生特定模
式命令接收中断。
   Bit[9]:保留；
   Bit[10]: 保留；
   Bit[11]：非法命令数据接收禁止配置；若非法命令数据接收禁止配置为‘0’时，对于
接收的非法子地址接收命令，控制流 NT 接收数据并写入到由子地址存储地址配置表制
定的 RAM 区；
   若非法命令数据接收禁止配置为‘1’时，对于接收的非法子地址接收命令，控制流
NT 舍弃接收的交换数据；
   Bit[12]：忙状态时交换数据接收禁止配置；若忙状态时交换数据接收禁止配置为‘0’
时，对于外部 NC 对本地处于忙状态接收子地址发起的访问时，控制流 NT 接收数据并
写入到由子地址存储地址配置表制定的 RAM 区；
   若忙状态时交换数据接收禁止配置为‘1’时，对于外部 NC 对本地处于忙状态接收子
地址发起的访问时，控制流 NT 舍弃接收的交换数据；
   Bit[13]: 保留；

                          第 49 页
   Bit[14]: 双缓冲使能配置；若双缓冲使能配置配置为‘0’，全部接收子地址均不能配
置为双缓存模式；若双缓冲使能配置配置为‘1’、且存储器管理增强模式配置为‘0’时，
全部接收子地址均配置为双缓存模式；
   若双缓冲使能配置配置为‘1’、且存储器管理增强模式配置为‘1’时，通过对子地址
控制字编程，可将每一个接收子地址配置为双缓存存储模式，或其他数据存储模式；
   Bit[15]: 状态帧中服务请求位自动清除配置；若状态帧中服务请求位自动清除配置
为‘0’，控制流 NT 返回状态帧中 service_request 位状态由 CtrlNT 配置存储器中 Service
request 位控制；
   若状态帧中服务请求位自动清除配置为‘1’，当控制流 NT 接收到一条 Transmit
Vector Word 模式命令时，控制流 NT 在返回的状态帧中 service request 位仍置为‘1’；在
交换结束时，控制流 NT 自动将 CtrlNT 配置存储器中 Service request 位写‘0’。

控制流 NT 配置寄存器 2
      偏移地址                           说明
       0x25    控制流 NT 配置寄存器 2
               Bit[1:0]: 命令堆栈深度配置；
                            "00",1KB(64 条交换);


########## fmt@3871 lines 3841-3930 ##########
                          1，增强模式；
                 Bit[8]:模式命令增强模式配置；
                          0，普通模式；
                          1，增强模式；
                 Bit[9]:保留；
                 Bit[10]: 保留；
                 Bit[11]:非法命令数据接收禁止配置；当命令非法时，是否禁止数据接
                 收写入到 RAM 区；
                          0，不禁止；
                          1，禁止；
                 Bit[12]:忙状态时交换数据接收禁止配置；当命令处于忙位时，是否
                 禁止数据接收写入到 RAM 区；
                          0，不禁止；
                          1，禁止；
                 Bit[13]:保留；
                 Bit[14]:双缓冲使能配置；
                 Bit[15]:服务请求位自动清除配置。
   Bit[2]:忙位设置是否对模式命令有效配置；若本位配置为‘0’、busy#(控制流 NT 配
置寄存器 2 中 Bit[2]配置为 0）或者命令忙位查找表使能且 busy 配置表中相应位配置为
‘1’时，对于接收的模式命令，NT 返回 BUSY 位置 1、且不带数据负荷的状态帧；若本
位配置为‘1’、busy#(控制流 NT 配置寄存器 2 中 Bit[2]配置为 0）或者命令忙位查找表使
能且 busy 配置表中相应位配置为‘1’时，对于接收的模式命令，NT 返回 BUSY 位置 1、
且带相应数据负荷的状态帧；
   Bit[3]: 在单缓冲/双缓冲模式下，地址遇到 512Byte 边界时是否禁止配置；若本位
配置为‘0’，在子地址配置为单缓冲/双缓冲模式下，GLink 分配给子地址数据存储容量
为固定值(512 字节)；在 GLink 进行与一次交换相对应的数据存储器访问、且存储器当
前访问地址为 18'hx_xx_ff 时，GLink 进行下一个访问的地址跳至 18'hx_xx_00；若本位
配置为‘1’，GLink 当前访问数据存储器的地址为 18'hx_xx_ff 时，GLink 进行下一个访
问的地址将直接递增 1，即{(10'hx_xx+1),8'h00}；
   Bit[4]: 保留；
   Bit[5]: 命令非法查找表功能是否禁止配置；GLinkNT 允许用户对于每个子地址进
行合法数据访问长度范围进行配置，长度范围为[1，512]字节；若接收到交换的数据字
节数不在用户配置的范围内，NT 判当前命令非法；
   Bit[6]: 命令忙位查找表是否使能配置；若本位配置为‘0’,控制流 NT 返回状态帧中
                            第 48 页
busy 位仅由 busy#(控制流 NT 配置寄存器 2 中 Bit[2]）决定;若本位配置为‘1’, 控制流
NT 返回状态帧中 busy 位置为 1 的条件包括 busy#(控制流 NT 配置寄存器 2 中 Bit[2]）
置 0,或者 busy 配置表中相应位配置为‘1’；
   Bit[7]: 存储器管理增强模式配置；若存储器管理增强模式配置为‘0’，控制流 NT
各子地址存储模式配置为单交换数据缓存模式或双交换数据缓存模式；对于发送子地址
数据，存储类型为单交换数据缓存模式；对于接收地址数据，当双缓冲使能位(控制流
NT 配置寄存器 1 的 bit[14])配置为 0 时，存储类型为单交换数据缓存模式，否则为双交
换数据缓存模式；
   当存储器管理增强模式配置为‘1’时，控制流 NT 子地址数据存储可配置为单交换数
据缓存模式、双交换数据缓存模式以及环形数据存储模式（支持存储深度为 2048 字节
至 131072 字节）；其中，双交换数据缓存模式仅适用于接收子地址，不适用于发送子
地址；
   Bit[8]: 模式命令增强模式配置；若模式命令增强模式配置为‘0’，用户可通过使能
控制流 NT 模式命令中断使能（中断屏蔽寄存器的 Bit[9]）来使能针对全部模式命令的
接收中断；发送模式命令和接收模式命令使用公共的数据存储地址区域；
   若模式命令增强模式配置为‘1’，可以通过配置模式命令中断选择表来产生特定模
式命令接收中断。
   Bit[9]:保留；
   Bit[10]: 保留；
   Bit[11]：非法命令数据接收禁止配置；若非法命令数据接收禁止配置为‘0’时，对于
接收的非法子地址接收命令，控制流 NT 接收数据并写入到由子地址存储地址配置表制
定的 RAM 区；
   若非法命令数据接收禁止配置为‘1’时，对于接收的非法子地址接收命令，控制流
NT 舍弃接收的交换数据；
   Bit[12]：忙状态时交换数据接收禁止配置；若忙状态时交换数据接收禁止配置为‘0’
时，对于外部 NC 对本地处于忙状态接收子地址发起的访问时，控制流 NT 接收数据并
写入到由子地址存储地址配置表制定的 RAM 区；
   若忙状态时交换数据接收禁止配置为‘1’时，对于外部 NC 对本地处于忙状态接收子
地址发起的访问时，控制流 NT 舍弃接收的交换数据；
   Bit[13]: 保留；

                          第 49 页
   Bit[14]: 双缓冲使能配置；若双缓冲使能配置配置为‘0’，全部接收子地址均不能配
置为双缓存模式；若双缓冲使能配置配置为‘1’、且存储器管理增强模式配置为‘0’时，
全部接收子地址均配置为双缓存模式；
   若双缓冲使能配置配置为‘1’、且存储器管理增强模式配置为‘1’时，通过对子地址
控制字编程，可将每一个接收子地址配置为双缓存存储模式，或其他数据存储模式；
   Bit[15]: 状态帧中服务请求位自动清除配置；若状态帧中服务请求位自动清除配置
为‘0’，控制流 NT 返回状态帧中 service_request 位状态由 CtrlNT 配置存储器中 Service
request 位控制；
   若状态帧中服务请求位自动清除配置为‘1’，当控制流 NT 接收到一条 Transmit
Vector Word 模式命令时，控制流 NT 在返回的状态帧中 service request 位仍置为‘1’；在
交换结束时，控制流 NT 自动将 CtrlNT 配置存储器中 Service request 位写‘0’。

控制流 NT 配置寄存器 2
      偏移地址                           说明
       0x25    控制流 NT 配置寄存器 2
               Bit[1:0]: 命令堆栈深度配置；
                            "00",1KB(64 条交换);
                            "01",2KB(128 条交换);
                            "10",4KB(256 条交换);
                            "11",8KB(512 条交换);
               Bit[2]:busy 位设置，
                             0，状态帧 busy 置 1；
                             1，状态帧 busy 置 0；


########## fmt@3880 lines 3850-3939 ##########
                          1，禁止；
                 Bit[12]:忙状态时交换数据接收禁止配置；当命令处于忙位时，是否
                 禁止数据接收写入到 RAM 区；
                          0，不禁止；
                          1，禁止；
                 Bit[13]:保留；
                 Bit[14]:双缓冲使能配置；
                 Bit[15]:服务请求位自动清除配置。
   Bit[2]:忙位设置是否对模式命令有效配置；若本位配置为‘0’、busy#(控制流 NT 配
置寄存器 2 中 Bit[2]配置为 0）或者命令忙位查找表使能且 busy 配置表中相应位配置为
‘1’时，对于接收的模式命令，NT 返回 BUSY 位置 1、且不带数据负荷的状态帧；若本
位配置为‘1’、busy#(控制流 NT 配置寄存器 2 中 Bit[2]配置为 0）或者命令忙位查找表使
能且 busy 配置表中相应位配置为‘1’时，对于接收的模式命令，NT 返回 BUSY 位置 1、
且带相应数据负荷的状态帧；
   Bit[3]: 在单缓冲/双缓冲模式下，地址遇到 512Byte 边界时是否禁止配置；若本位
配置为‘0’，在子地址配置为单缓冲/双缓冲模式下，GLink 分配给子地址数据存储容量
为固定值(512 字节)；在 GLink 进行与一次交换相对应的数据存储器访问、且存储器当
前访问地址为 18'hx_xx_ff 时，GLink 进行下一个访问的地址跳至 18'hx_xx_00；若本位
配置为‘1’，GLink 当前访问数据存储器的地址为 18'hx_xx_ff 时，GLink 进行下一个访
问的地址将直接递增 1，即{(10'hx_xx+1),8'h00}；
   Bit[4]: 保留；
   Bit[5]: 命令非法查找表功能是否禁止配置；GLinkNT 允许用户对于每个子地址进
行合法数据访问长度范围进行配置，长度范围为[1，512]字节；若接收到交换的数据字
节数不在用户配置的范围内，NT 判当前命令非法；
   Bit[6]: 命令忙位查找表是否使能配置；若本位配置为‘0’,控制流 NT 返回状态帧中
                            第 48 页
busy 位仅由 busy#(控制流 NT 配置寄存器 2 中 Bit[2]）决定;若本位配置为‘1’, 控制流
NT 返回状态帧中 busy 位置为 1 的条件包括 busy#(控制流 NT 配置寄存器 2 中 Bit[2]）
置 0,或者 busy 配置表中相应位配置为‘1’；
   Bit[7]: 存储器管理增强模式配置；若存储器管理增强模式配置为‘0’，控制流 NT
各子地址存储模式配置为单交换数据缓存模式或双交换数据缓存模式；对于发送子地址
数据，存储类型为单交换数据缓存模式；对于接收地址数据，当双缓冲使能位(控制流
NT 配置寄存器 1 的 bit[14])配置为 0 时，存储类型为单交换数据缓存模式，否则为双交
换数据缓存模式；
   当存储器管理增强模式配置为‘1’时，控制流 NT 子地址数据存储可配置为单交换数
据缓存模式、双交换数据缓存模式以及环形数据存储模式（支持存储深度为 2048 字节
至 131072 字节）；其中，双交换数据缓存模式仅适用于接收子地址，不适用于发送子
地址；
   Bit[8]: 模式命令增强模式配置；若模式命令增强模式配置为‘0’，用户可通过使能
控制流 NT 模式命令中断使能（中断屏蔽寄存器的 Bit[9]）来使能针对全部模式命令的
接收中断；发送模式命令和接收模式命令使用公共的数据存储地址区域；
   若模式命令增强模式配置为‘1’，可以通过配置模式命令中断选择表来产生特定模
式命令接收中断。
   Bit[9]:保留；
   Bit[10]: 保留；
   Bit[11]：非法命令数据接收禁止配置；若非法命令数据接收禁止配置为‘0’时，对于
接收的非法子地址接收命令，控制流 NT 接收数据并写入到由子地址存储地址配置表制
定的 RAM 区；
   若非法命令数据接收禁止配置为‘1’时，对于接收的非法子地址接收命令，控制流
NT 舍弃接收的交换数据；
   Bit[12]：忙状态时交换数据接收禁止配置；若忙状态时交换数据接收禁止配置为‘0’
时，对于外部 NC 对本地处于忙状态接收子地址发起的访问时，控制流 NT 接收数据并
写入到由子地址存储地址配置表制定的 RAM 区；
   若忙状态时交换数据接收禁止配置为‘1’时，对于外部 NC 对本地处于忙状态接收子
地址发起的访问时，控制流 NT 舍弃接收的交换数据；
   Bit[13]: 保留；

                          第 49 页
   Bit[14]: 双缓冲使能配置；若双缓冲使能配置配置为‘0’，全部接收子地址均不能配
置为双缓存模式；若双缓冲使能配置配置为‘1’、且存储器管理增强模式配置为‘0’时，
全部接收子地址均配置为双缓存模式；
   若双缓冲使能配置配置为‘1’、且存储器管理增强模式配置为‘1’时，通过对子地址
控制字编程，可将每一个接收子地址配置为双缓存存储模式，或其他数据存储模式；
   Bit[15]: 状态帧中服务请求位自动清除配置；若状态帧中服务请求位自动清除配置
为‘0’，控制流 NT 返回状态帧中 service_request 位状态由 CtrlNT 配置存储器中 Service
request 位控制；
   若状态帧中服务请求位自动清除配置为‘1’，当控制流 NT 接收到一条 Transmit
Vector Word 模式命令时，控制流 NT 在返回的状态帧中 service request 位仍置为‘1’；在
交换结束时，控制流 NT 自动将 CtrlNT 配置存储器中 Service request 位写‘0’。

控制流 NT 配置寄存器 2
      偏移地址                           说明
       0x25    控制流 NT 配置寄存器 2
               Bit[1:0]: 命令堆栈深度配置；
                            "00",1KB(64 条交换);
                            "01",2KB(128 条交换);
                            "10",4KB(256 条交换);
                            "11",8KB(512 条交换);
               Bit[2]:busy 位设置，
                             0，状态帧 busy 置 1；
                             1，状态帧 busy 置 0；
               Bit[3]:terminal flag 位设置，
                             0，状态帧 terminal flag 置 1；
                             1，状态帧 terminal flag 置 0；
               Bit[4]:subsystem flag 位设置，
                             0，状态帧 subsystem flag 置 1；
                             1，状态帧 subsystem flag 置 0；
               Bit[7:5]:保留；
               Bit[11:8]:与最近一次处理交换对应控制流 NT 编号；
               Bit[12]：由于 NCID 未匹配，控制流 NT 主动舍弃上一条交换状态；
